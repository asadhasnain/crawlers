# Crawler Samples in Python and C#

To run the sample install the following packages:

pip install requests
pip install beautifulsoup4

To run:

python.exe crawler.py

## C#
    Build and run CSharp\CSharpCrawler project

